database:
  type: postgres
  host: localhost
  port: 5432
  username: postgres
  password: postgres
  database: ai_brain
  synchronize: true
  logging: false
  entities:
    - dist/**/*.entity.js
  migrations:
    - dist/migrations/*.js
