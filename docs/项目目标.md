### 项目目标

### 构建角色和能力
- 每个角色都有名称、介绍说明、提示词定义
- 每个能力也有名称、介绍说明、提示词定义
- 能力是角色的扩展部分，例如：定义DBA角色，那么DBA角色的能力就有操作数据库，检查数据库SQL，检查数据库配置

### Agent综合解决方案
- agent是为了解决具体的问题而创建的解决方案
- agent可以包含多种角色，例如：DBA角色、项目经理，那么也会拥有调用角色能力的权限

### 线性时间线通知器
- 作为定时生命周期事件通知agent
- agent自己调用AI决策来决定执行哪些能力